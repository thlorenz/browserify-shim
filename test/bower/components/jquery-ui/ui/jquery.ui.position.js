(function( $, undefined ) {

$.ui = $.ui || {};

$.position = function () {
  return '$.position';
};

$.fn.position = function( options ) {
  return '$.fn.position';
};

$.ui.position = function () {
  return '$.ui.position';
};

}( jQuery ) );
