{
  "name": "shim-jquery-external",
  "version": "0.0.0",
  "description": "Example of using browserify-shim to shim jquery for use with browserify, using an external shim file",
  "main": "./js/entry.js",
  "browserify-shim": "./config/shim.js",
  "browserify": {
    "transform": [
      "browserify-shim"
    ]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "",
  "author": "Thorsten Lorenz",
  "license": "BSD",
  "dependencies": {
    "request": "~2.12.0"
  },
  "devDependencies": {
    "browserify": "~2.36.1",
    "browserify-shim": "~3.0.0"
  }
}
