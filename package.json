{
  "name": "browserify-shim",
  "version": "0.6.0",
  "description": "Shims any module so it can be browserified even if it isn't commonJS compatible.",
  "main": "index.js",
  "scripts": {
    "test": "tap test/*.js",
    "shim-jquery": "cd examples/shim-jquery && npm install && node build.js && open index.html",
    "shim-underscore": "cd examples/shim-underscore && npm install && node build.js && open index.html"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/thlorenz/browserify-shim.git"
  },
  "keywords": [
    "browserify",
    "shim",
    "jquery"
  ],
  "author": "Thorsten Lorenz <thlorenz@gmx.de> (thlorenz.com)",
  "license": "MIT",
  "readmeFilename": "README.md",
  "devDependencies": {
    "tap": "~0.3.3",
    "request": "~2.12.0",
    "jsdom": "~0.3.2",
    "browserify": "~2.3.1"
  },
  "dependencies": {
    "through": "~2.2.0"
  }
}
